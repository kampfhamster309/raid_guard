# Suricata IDS — configured in RAID-002b.
# Placeholder: image is pulled and validated here; PCAP input and rule
# configuration are added when the capture-agent FIFO is in place.
FROM jasonish/suricata:latest
